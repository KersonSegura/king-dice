# Force npm to install optional dependencies correctly
optional=true
# Use npm's legacy peer deps resolution to avoid conflicts
legacy-peer-deps=true
# Ensure proper platform-specific binaries
target_platform=linux
target_arch=x64
